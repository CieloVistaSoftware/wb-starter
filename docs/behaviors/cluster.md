# Cluster

The `x-cluster` behavior.

Apply `x-cluster` to any element.

## Usage

```html
<div x-cluster>
  …
</div>
```

## Classes applied

- `wb-cluster`

## Attributes read

- `gap`
- `justify`
- `align`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-cluster` on the [Behaviors showcase](/?page=behaviors) — search for `x-cluster` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `cluster`, so this file has only what the registry knows. Worth writing by hand.</sub>
