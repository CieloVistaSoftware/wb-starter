# Img

The `x-img` behavior.

Apply `x-img` to any element.

## Usage

```html
<div x-img>
  …
</div>
```

## Classes applied

- `wb-img`
- `wb-img--zoomable`

## Attributes read

- `placeholder`
- `fallback`
- `aspect-ratio`
- `src`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-img` on the [Behaviors showcase](/?page=behaviors) — search for `x-img` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `img`, so this file has only what the registry knows. Worth writing by hand.</sub>
