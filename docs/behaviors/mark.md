# Mark

The `x-mark` behavior.

Apply `x-mark` to any element.

## Usage

```html
<div x-mark>
  …
</div>
```

## Classes applied

- `wb-mark`

## Attributes read

- `variant`
- `color`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-mark` on the [Behaviors showcase](/?page=behaviors) — search for `x-mark` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `mark`, so this file has only what the registry knows. Worth writing by hand.</sub>
