# Pre

The `x-pre` behavior.

Apply `x-pre` to any element.

## Usage

```html
<div x-pre>
  …
</div>
```

## Classes applied

- `x-pre`
- `x-pre--has-header`
- `x-pre--wrap`
- `x-pre--has-max-height`
- `x-pre--has-line-numbers`
- `x-pre__line-numbers--has-header`

## Attributes read

- `language`
- `scrollable`
- `show-line-numbers`
- `max-height`
- `wrap`
- `size`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-pre` on the [Behaviors showcase](/?page=behaviors) — search for `x-pre` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `pre`, so this file has only what the registry knows. Worth writing by hand.</sub>
