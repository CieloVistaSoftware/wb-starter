# Flip

The `x-flip` behavior.

Apply `x-flip` to any element.

## Usage

```html
<button class="effect-demo" x-flip>Flip</button>
```

## What it does

On click, plays the `flip` animation for `0.6s` and removes it when it finishes.

## Live example

See `x-flip` on the [Behaviors showcase](/?page=behaviors) — search for `x-flip` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `flip`, so this file has only what the registry knows. Worth writing by hand.</sub>
