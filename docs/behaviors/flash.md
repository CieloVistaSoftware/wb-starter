# Flash

The `x-flash` behavior.

Apply `x-flash` to any element.

## Usage

```html
<button class="effect-demo" x-flash>Flash</button>
```

## What it does

On click, plays the `flash` animation for `0.75s` and removes it when it finishes.

## Live example

See `x-flash` on the [Behaviors showcase](/?page=behaviors) — search for `x-flash` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `flash`, so this file has only what the registry knows. Worth writing by hand.</sub>
