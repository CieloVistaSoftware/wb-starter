# Copybutton

The `x-copybutton` behavior.

Apply `x-copybutton` to any element.

## Usage

```html
<div x-copybutton>
  …
</div>
```

## Live example

See `x-copybutton` on the [Behaviors showcase](/?page=behaviors) — search for `x-copybutton` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `copybutton`, so this file has only what the registry knows. Worth writing by hand.</sub>
