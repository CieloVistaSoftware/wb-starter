# Breadcrumb

The `x-breadcrumb` behavior.

Apply `x-breadcrumb` to any element.

## Usage

```html
<nav x-breadcrumb items="Home,Products,Electronics,Smartphones"></nav>
```

## Classes applied

- `wb-breadcrumb`

## Attributes read

- `items`
- `separator`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-breadcrumb` on the [Behaviors showcase](/?page=behaviors) — search for `x-breadcrumb` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `breadcrumb`, so this file has only what the registry knows. Worth writing by hand.</sub>
