# Shake

The `x-shake` behavior.

Apply `x-shake` to any element.

## Usage

```html
<button class="effect-demo" x-shake>Shake</button>
```

## What it does

On click, plays the `shake` animation for `0.5s` and removes it when it finishes.

## Live example

See `x-shake` on the [Behaviors showcase](/?page=behaviors) — search for `x-shake` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `shake`, so this file has only what the registry knows. Worth writing by hand.</sub>
