# Vimeo

The `x-vimeo` behavior.

Apply `x-vimeo` to any element.

## Usage

```html
<div x-vimeo>
  …
</div>
```

## Classes applied

- `wb-vimeo`

## Attributes read

- `video-id`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-vimeo` on the [Behaviors showcase](/?page=behaviors) — search for `x-vimeo` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `vimeo`, so this file has only what the registry knows. Worth writing by hand.</sub>
