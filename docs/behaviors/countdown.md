# Countdown

The `x-countdown` behavior.

Apply `x-countdown` to any element.

## Usage

```html
<div x-countdown to="2027-12-31" class="time-display"></div>
```

## Classes applied

- `wb-countdown`
- `wb-countdown--complete`

## Attributes read

- `date`
- `to`
- `seconds`
- `format`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Events

- `wb:countdown:complete`

## Live example

See `x-countdown` on the [Behaviors showcase](/?page=behaviors) — search for `x-countdown` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `countdown`, so this file has only what the registry knows. Worth writing by hand.</sub>
