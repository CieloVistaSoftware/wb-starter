# Video

The `x-video` behavior.

Apply `x-video` to any element.

## Usage

```html
<div x-video>
  …
</div>
```

## Classes applied

- `wb-video`

## Attributes read

- `src`
- `controls`
- `poster`
- `playsinline`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-video` on the [Behaviors showcase](/?page=behaviors) — search for `x-video` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `video`, so this file has only what the registry knows. Worth writing by hand.</sub>
