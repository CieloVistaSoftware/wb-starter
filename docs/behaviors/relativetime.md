# Relativetime

The `x-relativetime` behavior.

Apply `x-relativetime` to any element.

## Usage

```html
<span x-relativetime date="2025-01-01" class="time-display">Jan 1, 2025</span>
```

## Classes applied

- `wb-relativetime`

## Attributes read

- `date`
- `datetime`
- `refresh`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-relativetime` on the [Behaviors showcase](/?page=behaviors) — search for `x-relativetime` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `relativetime`, so this file has only what the registry knows. Worth writing by hand.</sub>
