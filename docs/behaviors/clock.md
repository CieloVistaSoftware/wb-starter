# Clock

The `x-clock` behavior.

Apply `x-clock` to any element.

## Usage

```html
<div x-clock class="time-display"></div>
```

## Classes applied

- `wb-clock`

## Attributes read

- `variant`
- `format`
- `show-seconds`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-clock` on the [Behaviors showcase](/?page=behaviors) — search for `x-clock` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `clock`, so this file has only what the registry knows. Worth writing by hand.</sub>
