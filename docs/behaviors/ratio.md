# Ratio

The `x-ratio` behavior.

Apply `x-ratio` to any element.

## Usage

```html
<div x-ratio>
  …
</div>
```

## Classes applied

- `wb-ratio`

## Attributes read

- `ratio`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-ratio` on the [Behaviors showcase](/?page=behaviors) — search for `x-ratio` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `ratio`, so this file has only what the registry knows. Worth writing by hand.</sub>
