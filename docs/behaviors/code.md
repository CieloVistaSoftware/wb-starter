# Code

The `x-code` behavior.

Apply `x-code` to any element.

## Usage

```html
<code language="javascript">
// Debounce — delay a call until the caller stops firing.
export function debounce(fn, wait = 200) {
  let timer = null;
  return function debounced(...args) {
    clearTimeout(timer);
    timer = setTimeout(() =&gt; fn.apply(this, args), wait);
  };
}

// Throttle — run at most once per interval.
export function throttle(fn, interval = 100) {
  let last = 0;
  return function throttled(...args) {
    const now = Date.now();
    if (now - last &lt; interval) return;
    last = now;
    return fn.apply(this, args);
  };
}

const onResize = debounce(() =&gt; {
  const { innerWidth: w, innerHeight: h } = window;
  console.log(`viewport ${w}x${h}`);
}, 250);

window.addEventListener('resize', onResize);
</code>
```

## Classes applied

- `x-code`

## Attributes read

- `language`
- `variant`
- `scrollable`
- `size`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Events

- `wb:code:copy`

## Live example

See `x-code` on the [Behaviors showcase](/?page=behaviors) — search for `x-code` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `code`, so this file has only what the registry knows. Worth writing by hand.</sub>
