# Notify

The `x-notify` behavior.

Apply `x-notify` to any element.

## Usage

```html
<div x-notify>
  …
</div>
```

## Attributes read

- `message`
- `duration`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Events

- `wb:notify:show`

## Live example

See `x-notify` on the [Behaviors showcase](/?page=behaviors) — search for `x-notify` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `notify`, so this file has only what the registry knows. Worth writing by hand.</sub>
