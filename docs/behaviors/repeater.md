# Repeater

The `x-repeater` behavior.

Apply `x-repeater` to any element.

## Usage

```html
<div x-repeater>
  …
</div>
```

## Live example

See `x-repeater` on the [Behaviors showcase](/?page=behaviors) — search for `x-repeater` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `repeater`, so this file has only what the registry knows. Worth writing by hand.</sub>
