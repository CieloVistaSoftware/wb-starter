# Fullscreen

The `x-fullscreen` behavior.

Apply `x-fullscreen` to any element.

## Usage

```html
<button variant="secondary" x-fullscreen>⛶ Fullscreen</button>
```

## Classes applied

- `wb-fullscreen`

## Attributes read

- `target`
- `label`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-fullscreen` on the [Behaviors showcase](/?page=behaviors) — search for `x-fullscreen` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `fullscreen`, so this file has only what the registry knows. Worth writing by hand.</sub>
