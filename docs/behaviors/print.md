# Print

The `x-print` behavior.

Apply `x-print` to any element.

## Usage

```html
<button variant="secondary" x-print>🖨️ Print</button>
```

## Classes applied

- `wb-print`

## Attributes read

- `target`
- `label`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-print` on the [Behaviors showcase](/?page=behaviors) — search for `x-print` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `print`, so this file has only what the registry knows. Worth writing by hand.</sub>
