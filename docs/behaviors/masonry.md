# Masonry

The `x-masonry` behavior.

Apply `x-masonry` to any element.

## Usage

```html
<div x-masonry>
  …
</div>
```

## Classes applied

- `wb-masonry`

## Attributes read

- `columns`
- `gap`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-masonry` on the [Behaviors showcase](/?page=behaviors) — search for `x-masonry` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `masonry`, so this file has only what the registry knows. Worth writing by hand.</sub>
