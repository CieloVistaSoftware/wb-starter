# Flex

The `x-flex` behavior.

Apply `x-flex` to any element.

## Usage

```html
<div x-flex>
  …
</div>
```

## Classes applied

- `wb-flex`

## Attributes read

- `direction`
- `wrap`
- `justify`
- `align`
- `gap`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-flex` on the [Behaviors showcase](/?page=behaviors) — search for `x-flex` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `flex`, so this file has only what the registry knows. Worth writing by hand.</sub>
