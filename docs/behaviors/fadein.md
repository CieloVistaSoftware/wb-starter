# Fadein

On click, plays the fade-in animation for 0.5s.

Apply `x-fadein` to any element.

## Usage

```html
<button class="effect-demo" x-fadein>Fadein</button>
```

## Live example

See `x-fadein` on the [Behaviors showcase](/?page=behaviors) — search for `x-fadein` to run it and copy its markup.

---

<sub>Generated from `src/wb-models/fadein.schema.json` by `scripts/generate-behavior-docs.mjs` (#713). Attribute names, defaults and events are the declared ones. Expand this file by hand — the generator never overwrites an existing doc.</sub>
