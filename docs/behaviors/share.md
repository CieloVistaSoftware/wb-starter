# Share

The `x-share` behavior.

Apply `x-share` to any element.

## Usage

```html
<button variant="secondary" x-share share-title="wb-starter" share-url="https://example.com">📤 Share</button>
```

## Classes applied

- `wb-share`

## Attributes read

- `share-title`
- `title`
- `share-text`
- `text`
- `share-url`
- `url`
- `label`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-share` on the [Behaviors showcase](/?page=behaviors) — search for `x-share` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `share`, so this file has only what the registry knows. Worth writing by hand.</sub>
