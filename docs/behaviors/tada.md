# Tada

The `x-tada` behavior.

Apply `x-tada` to any element.

## Usage

```html
<button class="effect-demo" x-tada>Tada</button>
```

## What it does

On click, plays the `tada` animation for `1s` and removes it when it finishes.

## Live example

See `x-tada` on the [Behaviors showcase](/?page=behaviors) — search for `x-tada` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `tada`, so this file has only what the registry knows. Worth writing by hand.</sub>
