# Youtube

The `x-youtube` behavior.

Apply `x-youtube` to any element.

## Usage

```html
<div x-youtube id="dQw4w9WgXcQ" ratio="16:9"></div>
```

## Classes applied

- `wb-youtube`

## Attributes read

- `url`
- `video-id`
- `id`
- `controls`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-youtube` on the [Behaviors showcase](/?page=behaviors) — search for `x-youtube` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `youtube`, so this file has only what the registry knows. Worth writing by hand.</sub>
