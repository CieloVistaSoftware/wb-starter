# Control

The `x-control` behavior.

Apply `x-control` to any element.

## Usage

```html
<div x-control>
  …
</div>
```

## Live example

See `x-control` on the [Behaviors showcase](/?page=behaviors) — search for `x-control` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `control`, so this file has only what the registry knows. Worth writing by hand.</sub>
