# Typewriter

The `x-typewriter` behavior.

Apply `x-typewriter` to any element.

## Usage

```html
<div x-typewriter>
  …
</div>
```

## Classes applied

- `wb-typewriter`

## Attributes read

- `text`
- `speed`
- `cursor`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-typewriter` on the [Behaviors showcase](/?page=behaviors) — search for `x-typewriter` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `typewriter`, so this file has only what the registry knows. Worth writing by hand.</sub>
