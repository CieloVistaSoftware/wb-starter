# Jello

On click, plays the jello animation for 1s.

Apply `x-jello` to any element.

## Usage

```html
<button class="effect-demo" x-jello>Jello</button>
```

## Live example

See `x-jello` on the [Behaviors showcase](/?page=behaviors) — search for `x-jello` to run it and copy its markup.

---

<sub>Generated from `src/wb-models/jello.schema.json` by `scripts/generate-behavior-docs.mjs` (#713). Attribute names, defaults and events are the declared ones. Expand this file by hand — the generator never overwrites an existing doc.</sub>
