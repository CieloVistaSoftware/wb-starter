# Jello

The `x-jello` behavior.

Apply `x-jello` to any element.

## Usage

```html
<button class="effect-demo" x-jello>Jello</button>
```

## What it does

On click, plays the `jello` animation for `1s` and removes it when it finishes.

## Live example

See `x-jello` on the [Behaviors showcase](/?page=behaviors) — search for `x-jello` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `jello`, so this file has only what the registry knows. Worth writing by hand.</sub>
