# Glow

The `x-glow` behavior.

Apply `x-glow` to any element.

## Usage

```html
<button variant="primary" x-glow>💡 Glow</button>
```

## Classes applied

- `wb-glow`

## Attributes read

- `color`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-glow` on the [Behaviors showcase](/?page=behaviors) — search for `x-glow` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `glow`, so this file has only what the registry knows. Worth writing by hand.</sub>
