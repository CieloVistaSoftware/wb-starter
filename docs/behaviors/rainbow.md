# Rainbow

The `x-rainbow` behavior.

Apply `x-rainbow` to any element.

## Usage

```html
<button variant="primary" x-rainbow>🌈 Rainbow</button>
```

## Classes applied

- `wb-rainbow`

## Attributes read

- `duration`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-rainbow` on the [Behaviors showcase](/?page=behaviors) — search for `x-rainbow` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `rainbow`, so this file has only what the registry knows. Worth writing by hand.</sub>
