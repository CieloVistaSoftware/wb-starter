# Figure

The `x-figure` behavior.

Apply `x-figure` to any element.

## Usage

```html
<div x-figure>
  …
</div>
```

## Classes applied

- `wb-figure`
- `wb-figure--overlay`

## Attributes read

- `zoom`
- `lightbox`
- `caption-position`
- `caption`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-figure` on the [Behaviors showcase](/?page=behaviors) — search for `x-figure` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `figure`, so this file has only what the registry knows. Worth writing by hand.</sub>
