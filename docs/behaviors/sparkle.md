# Sparkle

The `x-sparkle` behavior.

Apply `x-sparkle` to any element.

## Usage

```html
<button variant="primary" x-sparkle>✨ Sparkle</button>
```

## Classes applied

- `wb-sparkle-trigger`
- `wb-sparkle`

## Attributes read

- `count`

<sub>Taken from the behavior source — these are the attribute names it actually reads.</sub>

## Live example

See `x-sparkle` on the [Behaviors showcase](/?page=behaviors) — search for `x-sparkle` to run it and copy its markup.

---

<sub>Stub generated by `scripts/generate-behavior-docs.mjs` (#713). There is no schema for `sparkle`, so this file has only what the registry knows. Worth writing by hand.</sub>
