{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "container.schema.json",
  "title": "Container",
  "description": "Behavior applied with x-container.",
  "schemaFor": "container",
  "properties": {
    "direction": {
      "type": "string",
      "description": "Read by container().",
      "default": "column"
    },
    "columns": {
      "type": "string",
      "description": "Read by container().",
      "default": "1"
    },
    "gap": {
      "type": "string",
      "description": "Read by container().",
      "default": "1rem"
    },
    "align": {
      "type": "string",
      "description": "Read by container().",
      "default": "stretch"
    },
    "justify": {
      "type": "string",
      "description": "Read by container().",
      "default": "start"
    },
    "padding": {
      "type": "string",
      "description": "Read by container().",
      "default": "1rem"
    },
    "max-width": {
      "type": "string",
      "description": "Read by container().",
      "default": "this is the max width"
    },
    "wrap": {
      "type": "string",
      "description": "Read by container().",
      "default": "this is the wrap"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-container"
  },
  "test": {
    "setup": [
      "<div x-container>Test Content</div>",
      "<div x-container direction=\"column\">Test Content</div>",
      "<div x-container columns=\"1\">Test Content</div>",
      "<div x-container gap=\"1rem\">Test Content</div>",
      "<div x-container align=\"stretch\">Test Content</div>",
      "<div x-container justify=\"start\">Test Content</div>",
      "<div x-container padding=\"1rem\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
