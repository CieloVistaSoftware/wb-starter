{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "steps.schema.json",
  "title": "Steps",
  "description": "Behavior applied with x-steps.",
  "schemaFor": "x-steps",
  "properties": {
    "items": {
      "type": "string",
      "description": "Comma-separated step labels, e.g. `Details,Payment,Confirm`. Whitespace around each is trimmed.",
      "default": "this is the items"
    },
    "current": {
      "type": "string",
      "description": "Which step is active, counting from **1**, not 0. Defaults to `1`.",
      "default": "1"
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
