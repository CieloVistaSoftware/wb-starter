{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "relativetime.schema.json",
  "title": "Relativetime",
  "description": "Behavior applied with x-relativetime.",
  "schemaFor": "x-relativetime",
  "properties": {
    "datetime": {
      "type": "string",
      "description": "Alias of `date`, read only when `date` is absent.",
      "default": ""
    },
    "refresh": {
      "type": "string",
      "description": "How often the text is recomputed, in milliseconds. Defaults to `60000` — once a minute.",
      "default": "60000"
    },
    "date": {
      "type": "string",
      "description": "The timestamp to describe. `datetime` is accepted as an alias; `date` is read first."
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
