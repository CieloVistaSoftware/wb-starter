{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "grid.schema.json",
  "title": "Grid",
  "description": "Behavior applied with x-grid.",
  "schemaFor": "grid",
  "properties": {
    "columns": {
      "type": "string",
      "description": "Read by grid().",
      "default": "3"
    },
    "rows": {
      "type": "string",
      "description": "Read by grid().",
      "default": "this is the rows"
    },
    "gap": {
      "type": "string",
      "description": "Read by grid().",
      "default": "1rem"
    },
    "min-width": {
      "type": "string",
      "description": "Read by grid().",
      "default": "this is the min width"
    },
    "align": {
      "type": "string",
      "description": "Read by grid().",
      "default": "this is the align"
    },
    "justify": {
      "type": "string",
      "description": "Read by grid().",
      "default": "this is the justify"
    },
    "background": {
      "type": "string",
      "description": "Read by grid().",
      "default": "this is the background"
    },
    "headers": {
      "type": "string",
      "description": "Read by grid().",
      "default": "this is the headers"
    },
    "center": {
      "type": "boolean",
      "description": "Read by grid(). Bare attribute.",
      "default": false
    },
    "alt-rows": {
      "type": "boolean",
      "description": "Read by grid(). Bare attribute.",
      "default": false
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-grid"
  },
  "test": {
    "setup": [
      "<div x-grid>Test Content</div>",
      "<div x-grid columns=\"3\">Test Content</div>",
      "<div x-grid gap=\"1rem\">Test Content</div>",
      "<div x-grid center>Test Content</div>",
      "<div x-grid alt-rows>Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
