{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "vimeo.schema.json",
  "title": "Vimeo",
  "description": "Behavior applied with x-vimeo.",
  "schemaFor": "x-vimeo",
  "properties": {
    "video-id": {
      "type": "string",
      "description": "The numeric Vimeo id — the trailing digits of a vimeo.com URL. This is what identifies the video; without it there is nothing to embed."
    },
    "autoplay": {
      "type": "boolean",
      "description": "Begin playing on load. Browsers block autoplay with sound, so it needs `muted` to work unattended.",
      "default": false
    },
    "muted": {
      "type": "boolean",
      "description": "Start with audio silenced. Required for `autoplay` to be permitted.",
      "default": false
    },
    "loop": {
      "type": "boolean",
      "description": "Restart from the beginning when playback ends.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
