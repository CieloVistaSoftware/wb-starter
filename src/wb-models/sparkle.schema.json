{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sparkle.schema.json",
  "title": "Sparkle",
  "description": "Behavior applied with x-sparkle.",
  "schemaFor": "sparkle",
  "properties": {
    "count": {
      "type": "string",
      "description": "How many sparkle particles to emit. The behavior's default is `15`.",
      "default": "15"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-sparkle"
  },
  "test": {
    "setup": [
      "<div x-sparkle>Test Content</div>",
      "<div x-sparkle count=\"15\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
