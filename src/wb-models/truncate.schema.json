{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "truncate.schema.json",
  "title": "Truncate",
  "description": "Behavior applied with x-truncate.",
  "schemaFor": "x-truncate",
  "properties": {
    "lines": {
      "type": "string",
      "description": "Number of lines to clamp to before truncating. Defaults to `1`.",
      "default": "1"
    },
    "expandable": {
      "type": "boolean",
      "description": "Add a control that reveals the full text. Bare attribute — the plain spelling only began working in #752; before that `data-expandable` was the only form read.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
