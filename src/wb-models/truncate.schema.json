{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "truncate.schema.json",
  "title": "Truncate",
  "description": "Behavior applied with x-truncate.",
  "schemaFor": "x-truncate",
  "properties": {
    "lines": {
      "type": "string",
      "description": "Read by truncate().",
      "default": "1"
    },
    "expandable": {
      "type": "boolean",
      "description": "Read by truncate(). Bare attribute.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
