{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "cover.schema.json",
  "title": "Cover",
  "description": "Behavior applied with x-cover.",
  "schemaFor": "cover",
  "properties": {
    "min-height": {
      "type": "string",
      "description": "Read by cover().",
      "default": "100vh"
    },
    "padding": {
      "type": "string",
      "description": "Read by cover().",
      "default": "1rem"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-cover"
  },
  "test": {
    "setup": [
      "<div x-cover>Test Content</div>",
      "<div x-cover min-height=\"100vh\">Test Content</div>",
      "<div x-cover padding=\"1rem\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
