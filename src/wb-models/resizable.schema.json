{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "resizable.schema.json",
  "title": "Resizable",
  "behavior": "resizable",
  "description": "Make an element resizable.",
  "properties": {
      "handles": { "type": "string", "default": "se" }
  }
}
