{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "typewriter.schema.json",
  "title": "Typewriter",
  "description": "Behavior applied with x-typewriter.",
  "schemaFor": "x-typewriter",
  "properties": {
    "speed": {
      "type": "string",
      "description": "Milliseconds between characters. Defaults to `50` — lower is faster.",
      "default": "50"
    },
    "text": {
      "type": "string",
      "description": "Text to type out. Falls back to the element's existing `textContent`, so it can be left off when the content is already in the markup."
    },
    "cursor": {
      "type": "string",
      "description": "Show the blinking cursor. On unless set to `\"false\"`."
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
