{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "masonry.schema.json",
  "title": "Masonry",
  "description": "Behavior applied with x-masonry.",
  "schemaFor": "masonry",
  "properties": {
    "columns": {
      "type": "string",
      "description": "Number of masonry columns. Defaults to `3`.",
      "default": "3"
    },
    "gap": {
      "type": "string",
      "description": "Space between items, as a CSS length. Defaults to `1rem`.",
      "default": "1rem"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-masonry"
  },
  "test": {
    "setup": [
      "<div x-masonry>Test Content</div>",
      "<div x-masonry columns=\"3\">Test Content</div>",
      "<div x-masonry gap=\"1rem\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
