{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Counter Behavior",
  "type": "object",
  "description": "Schema for x-counter behavior (character counter)",
  "properties": {
    "max": { "type": "integer", "description": "Maximum character count" },
    "warning": { "type": "integer", "description": "Warning threshold for character count" }
  },
  "required": [],
  "wbBehavior": {
    "name": "counter",
    "type": "modifier",
    "category": "inputs",
    "description": "Displays a live character counter for inputs.",
    "src": "src/wb-viewmodels/counter.js"
  }
}
