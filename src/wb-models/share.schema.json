{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "share.schema.json",
  "title": "Share",
  "description": "Behavior applied with x-share.",
  "schemaFor": "x-share",
  "properties": {
    "text": {
      "type": "string",
      "description": "Read by share().",
      "default": ""
    },
    "label": {
      "type": "string",
      "description": "Read by share().",
      "default": "📤 Share"
    },
    "share-title": {
      "type": "string",
      "description": "Read by share()."
    },
    "share-text": {
      "type": "string",
      "description": "Read by share()."
    },
    "share-url": {
      "type": "string",
      "description": "Read by share()."
    },
    "url": {
      "type": "string",
      "description": "Read by share()."
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
