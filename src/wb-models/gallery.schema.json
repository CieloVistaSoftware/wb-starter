{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gallery.schema.json",
  "title": "Gallery",
  "description": "Behavior applied with x-gallery.",
  "schemaFor": "x-gallery",
  "properties": {
    "columns": {
      "type": "string",
      "description": "Number of columns in the grid. Defaults to `3`.",
      "default": "3"
    },
    "size": {
      "type": "string",
      "description": "Fixed thumbnail size (e.g. `150px`), independent of the column count — use it when you want uniform tiles rather than columns dividing the width.",
      "default": ""
    },
    "gap": {
      "type": "string",
      "description": "Space between items, as a CSS length. Defaults to `1rem`.",
      "default": "1rem"
    },
    "lightbox": {
      "type": "string",
      "description": "Clicking any item opens it full-size in a lightbox."
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
