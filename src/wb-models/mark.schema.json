{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mark.schema.json",
  "title": "Mark",
  "description": "Behavior applied with x-mark.",
  "schemaFor": "x-mark",
  "properties": {
    "variant": {
      "type": "string",
      "description": "Semantic highlight colour: `success`, `warning`, `danger` or `info`. **Ignored when `color` is set** — an explicit colour wins."
    },
    "color": {
      "type": "string",
      "description": "Any CSS colour, used as the highlight background. The text colour is computed from its luminance so the mark stays readable, and setting this overrides `variant`."
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
