{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "img.schema.json",
  "title": "Img",
  "description": "Behavior applied with x-img.",
  "schemaFor": "x-img",
  "properties": {
    "placeholder": {
      "type": "string",
      "description": "Image shown while the real `src` loads. Replaced the moment the real image decodes.",
      "default": ""
    },
    "fallback": {
      "type": "string",
      "description": "Image swapped in when `src` fails to load. Without one a broken image raises a loggable error and leaves the element empty.",
      "default": ""
    },
    "aspect-ratio": {
      "type": "string",
      "description": "A CSS aspect ratio (e.g. `16/9`) applied to the element, with `object-fit: cover`. Reserves the box before the image arrives, so the page does not jump as it loads.",
      "default": ""
    },
    "lazy": {
      "type": "boolean",
      "description": "Sets `loading=\"lazy\"`, so the browser defers fetching until the image nears the viewport. Bare attribute.",
      "default": false
    },
    "zoomable": {
      "type": "boolean",
      "description": "Clicking the image opens it full-size in a lightbox. Bare attribute.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
