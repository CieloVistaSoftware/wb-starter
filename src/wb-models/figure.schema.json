{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "figure.schema.json",
  "title": "Figure",
  "description": "Behavior applied with x-figure.",
  "schemaFor": "x-figure",
  "properties": {
    "caption-position": {
      "type": "string",
      "description": "Where the caption sits: `bottom` (default) places it beneath the image; `overlay` positions it absolutely across the bottom of the image on a translucent dark bar.",
      "default": "bottom"
    },
    "zoom": {
      "type": "boolean",
      "description": "Clicking the image opens it at full size. Bare attribute.",
      "default": false
    },
    "lightbox": {
      "type": "string",
      "description": "Open the image in a lightbox on click. **On by default** — this is opt-OUT, so write `lightbox=\"false\"` to disable it.",
      "default": "this is the lightbox"
    },
    "caption": {
      "type": "string",
      "description": "Caption text. Sets the `<figcaption>` content, creating one if the figure has none.",
      "default": "this is the caption"
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
