{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "release.schema.json",
  "title": "Release",
  "description": "Behavior applied with x-release.",
  "schemaFor": "x-release",
  "properties": {
    "format": {
      "type": "string",
      "description": "Template for the badge text, with `{version}` replaced. Defaults to `v{version}`.",
      "default": "v{version}"
    },
    "reload": {
      "type": "string",
      "description": "Clear caches and reload when the badge is activated. On unless set to `\"false\"`.",
      "default": "this is the reload"
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
