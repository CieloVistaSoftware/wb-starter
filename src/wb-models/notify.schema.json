{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "notify.schema.json",
  "title": "Notify",
  "description": "Behavior applied with x-notify.",
  "schemaFor": "x-notify",
  "properties": {
    "message": {
      "type": "string",
      "description": "Text of the notification. Defaults to `Notification`.",
      "default": "Notification"
    },
    "duration": {
      "type": "string",
      "description": "How long the notification stays up, in milliseconds. Defaults to `3000`.",
      "default": "3000"
    }
  },
  "events": {
    "wb:notify:show": "Fired by notify()."
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
