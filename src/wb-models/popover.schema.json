{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "popover.schema.json",
  "title": "Popover",
  "description": "Behavior applied with x-popover.",
  "schemaFor": "x-popover",
  "properties": {
    "description": {
      "type": "string",
      "description": "Read by popover().",
      "default": ""
    },
    "heading": {
      "type": "string",
      "description": "Read by popover().",
      "default": ""
    },
    "trigger": {
      "type": "string",
      "description": "Read by popover().",
      "default": "click"
    },
    "position": {
      "type": "string",
      "description": "Read by popover().",
      "default": "top"
    },
    "popover-content": {
      "type": "string",
      "description": "Read by popover()."
    },
    "popover-title": {
      "type": "string",
      "description": "Read by popover()."
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
