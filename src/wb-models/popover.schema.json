{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "popover.schema.json",
  "title": "Popover",
  "description": "Behavior applied with x-popover.",
  "schemaFor": "x-popover",
  "properties": {
    "description": {
      "type": "string",
      "description": "Popover body text. Only used when `popover-content` is absent.",
      "default": ""
    },
    "heading": {
      "type": "string",
      "description": "Popover heading. Only used when `popover-title` is absent.",
      "default": ""
    },
    "trigger": {
      "type": "string",
      "description": "What opens it: `click` (default) or `hover`.",
      "default": "click"
    },
    "position": {
      "type": "string",
      "description": "Side the popover opens on: `top` (default), `bottom`, `left` or `right`.",
      "default": "top"
    },
    "popover-content": {
      "type": "string",
      "description": "Popover body text. Read BEFORE `description`."
    },
    "popover-title": {
      "type": "string",
      "description": "Popover heading. Read BEFORE `heading`."
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
