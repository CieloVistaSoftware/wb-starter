{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "clock.schema.json",
  "title": "Clock",
  "description": "Behavior applied with x-clock.",
  "schemaFor": "clock",
  "properties": {
    "variant": {
      "type": "string",
      "description": "Clock face: `digital` (default) or the analogue rendering.",
      "default": "digital"
    },
    "format": {
      "type": "string",
      "description": "`24` (default) or `12` for a 12-hour clock with AM/PM.",
      "default": "24"
    },
    "show-seconds": {
      "type": "string",
      "description": "Show the seconds field. On unless set to `\"false\"`.",
      "default": "this is the show seconds"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-clock"
  },
  "test": {
    "setup": [
      "<div x-clock>Test Content</div>",
      "<div x-clock variant=\"digital\">Test Content</div>",
      "<div x-clock format=\"24\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
