{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "youtube.schema.json",
  "title": "Youtube",
  "description": "Behavior applied with x-youtube.",
  "schemaFor": "x-youtube",
  "properties": {
    "url": {
      "type": "string",
      "description": "Read by youtube()."
    },
    "video-id": {
      "type": "string",
      "description": "Read by youtube()."
    },
    "controls": {
      "type": "string",
      "description": "Read by youtube()."
    },
    "autoplay": {
      "type": "boolean",
      "description": "Read by youtube(). Bare attribute.",
      "default": false
    },
    "muted": {
      "type": "boolean",
      "description": "Read by youtube(). Bare attribute.",
      "default": false
    },
    "loop": {
      "type": "boolean",
      "description": "Read by youtube(). Bare attribute.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
