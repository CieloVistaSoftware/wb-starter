{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "icon.schema.json",
  "title": "Icon",
  "description": "Behavior applied with x-icon.",
  "schemaFor": "icon",
  "properties": {
    "size": {
      "type": "string",
      "description": "Read by icon().",
      "default": "1em"
    },
    "space": {
      "type": "string",
      "description": "Read by icon().",
      "default": "0.5em"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-icon"
  },
  "test": {
    "setup": [
      "<div x-icon>Test Content</div>",
      "<div x-icon size=\"1em\">Test Content</div>",
      "<div x-icon space=\"0.5em\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
