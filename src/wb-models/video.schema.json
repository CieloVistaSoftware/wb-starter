{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "video.schema.json",
  "title": "Video",
  "description": "Behavior applied with x-video.",
  "schemaFor": "x-video",
  "properties": {
    "poster": {
      "type": "string",
      "description": "Image shown before playback starts, and after the video ends. Without one the first frame is used, which is often black.",
      "default": "#"
    },
    "controls": {
      "type": "string",
      "description": "Show the browser's native play/pause/seek controls. Without them the video can only be driven by script.",
      "default": "this is the controls"
    },
    "playsinline": {
      "type": "string",
      "description": "Play inline on mobile rather than taking over the screen. iOS goes fullscreen by default without it.",
      "default": "this is the playsinline"
    },
    "autoplay": {
      "type": "boolean",
      "description": "Begin playing on load. Browsers block autoplay with sound, so this needs `muted` to work unattended.",
      "default": false
    },
    "muted": {
      "type": "boolean",
      "description": "Start with the audio track silenced. Required for `autoplay` to be allowed.",
      "default": false
    },
    "loop": {
      "type": "boolean",
      "description": "Restart from the beginning when playback reaches the end.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
