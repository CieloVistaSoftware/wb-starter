{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "video.schema.json",
  "title": "Video",
  "description": "Behavior applied with x-video.",
  "schemaFor": "x-video",
  "properties": {
    "poster": {
      "type": "string",
      "description": "Read by video().",
      "default": ""
    },
    "controls": {
      "type": "string",
      "description": "Read by video()."
    },
    "playsinline": {
      "type": "string",
      "description": "Read by video()."
    },
    "autoplay": {
      "type": "boolean",
      "description": "Read by video(). Bare attribute.",
      "default": false
    },
    "data-autoplay": {
      "type": "boolean",
      "description": "Read by video(). Bare attribute.",
      "default": false
    },
    "muted": {
      "type": "boolean",
      "description": "Read by video(). Bare attribute.",
      "default": false
    },
    "data-muted": {
      "type": "boolean",
      "description": "Read by video(). Bare attribute.",
      "default": false
    },
    "loop": {
      "type": "boolean",
      "description": "Read by video(). Bare attribute.",
      "default": false
    },
    "data-loop": {
      "type": "boolean",
      "description": "Read by video(). Bare attribute.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
