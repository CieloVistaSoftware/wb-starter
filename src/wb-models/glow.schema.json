{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "glow.schema.json",
  "title": "Glow",
  "description": "Behavior applied with x-glow.",
  "schemaFor": "glow",
  "properties": {
    "color": {
      "type": "string",
      "description": "Colour of the glow. Defaults to the theme primary (`var(--primary, #6366f1)`), so it follows the active theme unless you pin it.",
      "default": "var(--primary, #6366f1)"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-glow"
  },
  "test": {
    "setup": [
      "<div x-glow>Test Content</div>",
      "<div x-glow color=\"var(--primary, #6366f1)\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
