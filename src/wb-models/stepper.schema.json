{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Stepper Behavior",
  "type": "object",
  "description": "Schema for x-stepper behavior (numeric stepper)",
  "properties": {
    "min": { "type": "number", "description": "Minimum value" },
    "max": { "type": "number", "description": "Maximum value" },
    "step": { "type": "number", "description": "Step increment" }
  },
  "required": [],
  "wbBehavior": {
    "name": "stepper",
    "type": "modifier",
    "category": "inputs",
    "description": "Adds increment/decrement controls to numeric inputs.",
    "src": "src/wb-viewmodels/stepper.js"
  }
}
