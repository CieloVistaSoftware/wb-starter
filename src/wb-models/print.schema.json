{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "print.schema.json",
  "title": "Print",
  "description": "Behavior applied with x-print.",
  "schemaFor": "print",
  "properties": {
    "target": {
      "type": "string",
      "description": "CSS selector for the region to print. Empty prints the whole page.",
      "default": "this is the target"
    },
    "label": {
      "type": "string",
      "description": "Button label. Defaults to `🖨️ Print`.",
      "default": "🖨️ Print"
    }
  },
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "compliance": {
    "baseClass": "x-print"
  },
  "test": {
    "setup": [
      "<div x-print>Test Content</div>",
      "<div x-print label=\"🖨️ Print\">Test Content</div>"
    ]
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
