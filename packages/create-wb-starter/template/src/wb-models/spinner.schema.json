{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "spinner.schema.json",
  "title": "Spinner",
  "description": "Loading spinner indicator",
  "schemaFor": "spinner",
  "baseClass": "x-spinner",
  "semanticElement": {
    "tagName": "div",
    "implicitRole": "status"
  },
  "compliance": {
    "baseClass": "x-spinner",
    "supportLevel": "stable",
    "accessibility": "high"
  },
  "properties": {
    "size": {
      "type": "string",
      "enum": [
        "xs",
        "sm",
        "md",
        "lg",
        "xl"
      ],
      "default": "md",
      "appliesClass": "x-spinner--{{value}}"
    },
    "variant": {
      "type": "string",
      "enum": [
        "default",
        "primary",
        "success",
        "warning",
        "error"
      ],
      "default": "primary",
      "appliesClass": "x-spinner--{{value}}"
    },
    "speed": {
      "type": "string",
      "enum": [
        "slow",
        "medium",
        "fast"
      ],
      "default": "medium",
      "appliesClass": "x-spinner--{{value}}"
    },
    "label": {
      "type": "string",
      "description": "Accessible label",
      "default": "Loading"
    }
  },
  "$view": [
    {
      "name": "spinner",
      "tag": "div",
      "public": true,
      "required": true,
      "description": "Spinning element"
    }
  ],
  "$methods": {
    "show": {
      "description": "Shows the spinner",
      "params": []
    },
    "hide": {
      "description": "Hides the spinner",
      "params": []
    }
  },
  "$cssAPI": {
    "--x-spinner-size": {
      "default": "24px",
      "description": "Spinner size"
    },
    "--x-spinner-border-width": {
      "default": "3px",
      "description": "Border width"
    },
    "--x-spinner-color": {
      "default": "var(--primary, #6366f1)",
      "description": "Spinner color"
    },
    "--x-spinner-track-color": {
      "default": "rgba(0,0,0,0.1)",
      "description": "Track color"
    },
    "--x-spinner-speed": {
      "default": "0.8s",
      "description": "Animation duration"
    }
  },
  "accessibility": {
    "role": "status",
    "ariaLabel": "dynamic from label"
  },
  "test": {
    "setup": [
      "<span x-spinner>Basic spinner content</span>",
      "<span x-spinner size=\"xs\">size=xs</span>",
      "<span x-spinner size=\"sm\">size=sm</span>",
      "<span x-spinner size=\"md\">size=md</span>",
      "<span x-spinner variant=\"default\">variant=default</span>",
      "<span x-spinner variant=\"primary\">variant=primary</span>",
      "<span x-spinner variant=\"success\">variant=success</span>",
      "<span x-spinner speed=\"slow\">speed=slow</span>"
    ],
    "matrix": {
      "combinations": [
        {},
        {
          "size": "sm"
        },
        {
          "size": "lg"
        },
        {
          "variant": "success"
        },
        {
          "speed": "fast"
        }
      ]
    },
    "site": {
      "page": "feedback",
      "tag": "x-spinner",
      "renders": true,
      "baseClass": "x-spinner",
      "minInstances": 5,
      "checkText": [],
      "checkClasses": [
        "x-spinner--xs",
        "x-spinner--default",
        "x-spinner--slow"
      ],
      "checkAttributes": []
    }
  },
  "_metadata": {
    "category": "feedback",
    "icon": "⏳",
    "since": "1.0.0",
    "status": "stable"
  }
}
