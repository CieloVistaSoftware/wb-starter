{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "searchfield.schema.json",
  "title": "Searchfield",
  "description": "Behavior applied with x-searchfield.",
  "schemaFor": "x-searchfield",
  "properties": {
    "size": {
      "type": "string",
      "description": "Read by searchfield().",
      "default": "md"
    },
    "variant": {
      "type": "string",
      "description": "Read by searchfield().",
      "default": "default"
    },
    "placeholder": {
      "type": "string",
      "description": "Read by searchfield().",
      "default": "Search..."
    },
    "debounce": {
      "type": "string",
      "description": "Read by searchfield().",
      "default": "300"
    },
    "icon": {
      "type": "string",
      "description": "Read by searchfield().",
      "default": "🔍"
    },
    "clearable": {
      "type": "string",
      "description": "Read by searchfield()."
    },
    "disabled": {
      "type": "boolean",
      "description": "Read by searchfield(). Bare attribute.",
      "default": false
    },
    "loading": {
      "type": "boolean",
      "description": "Read by searchfield(). Bare attribute.",
      "default": false
    },
    "instant": {
      "type": "boolean",
      "description": "Read by searchfield(). Bare attribute.",
      "default": false
    }
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
