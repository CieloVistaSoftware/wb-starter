{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Counter Behavior",
  "type": "object",
  "description": "Schema for x-counter behavior (character counter)",
  "properties": {
    "max": { "type": "integer", "description": "Maximum character count", "default": 0 },
    "warning": { "type": "integer", "description": "Warning threshold for character count", "default": 0 }
  },
  "required": [],
  "schemaFor": "counter",
  "$view": [],
  "$methods": {},
  "$cssAPI": {},
  "wbBehavior": {
    "name": "counter",
    "type": "modifier",
    "category": "inputs",
    "description": "Displays a live character counter for inputs.",
    "src": "src/wb-viewmodels/counter.js"
  }
}
