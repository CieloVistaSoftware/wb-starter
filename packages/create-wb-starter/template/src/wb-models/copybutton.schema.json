{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "copybutton.schema.json",
  "title": "Copybutton",
  "description": "Behavior applied with x-copybutton.",
  "schemaFor": "x-copybutton",
  "properties": {
    "x-copybutton": {
      "type": "string",
      "description": "Read by copybutton().",
      "default": ""
    },
    "label": {
      "type": "string",
      "description": "Read by copybutton().",
      "default": "Copy"
    },
    "position": {
      "type": "string",
      "description": "Read by copybutton().",
      "default": "top-right"
    },
    "copy-feedback": {
      "type": "string",
      "description": "Read by copybutton().",
      "default": "Copied ✓"
    },
    "copy-duration": {
      "type": "string",
      "description": "Read by copybutton().",
      "default": "2000"
    },
    "copy-target": {
      "type": "string",
      "description": "Read by copybutton()."
    }
  },
  "events": {
    "wb:copy:success": "Fired by copybutton().",
    "wb:copy:error": "Fired by copybutton()."
  },
  "_metadata": {
    "generatedBy": "scripts/generate-behavior-schemas.mjs (#716)",
    "derivedFrom": "the behavior implementation — every property here is one the code actually reads",
    "note": "Expand by hand: enums, richer descriptions and accessibility notes cannot be read from source."
  }
}
