# Slider

Image comparison slider or carousel.

## Type — new capability

`x-slider` adds behavior that no HTML element implies. Nothing about a tag says "ripple" or "tooltip", so this is always opted into by attribute, on whatever element you already chose.

### How to write it

```html
<input x-slider type="range" min="0" max="100" value="35">
```

## Live example

See `x-slider` on the [Behaviors showcase](/?page=behaviors) — search for `x-slider` to run it and copy its markup.

---

<sub>Generated from `src/wb-models/slider.schema.json` by `scripts/generate-behavior-docs.mjs` (#713). Attribute names, defaults and events are the declared ones. Expand this file by hand — the generator never overwrites an existing doc.</sub>
