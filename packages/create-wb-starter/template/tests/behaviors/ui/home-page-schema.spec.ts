/**
 * REMOVED: Merged into home-page-permutation.spec.ts (schema-driven)
 */
import { test } from '@playwright/test';
test.describe.skip('Home Page Schema — merged into permutation test', () => {
  test('placeholder', () => {});
});
